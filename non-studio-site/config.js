// Fill these in from Supabase → Project Settings → API Keys.
// Use the PUBLISHABLE key (sb_publishable_...) or the legacy anon key. Never the secret key.
// The anon key is safe to expose in the browser; access is limited by Row Level Security.
window.NON_CONFIG = {
  SUPABASE_URL: 'https://YOUR-PROJECT.supabase.co',
  SUPABASE_ANON_KEY: 'sb_publishable_YOUR-KEY'
};
